# Use the official PHP image with Apache
FROM php:apache

# Copy the local index.php file to the container's web root
COPY . /var/www/html/

# Set the working directory
WORKDIR /var/www/html

# Expose port 80
EXPOSE 8080
